<view class="i-class i-radio" catchtap="radioChange">
  <label>
      <radio value="{{value}}" checked="{{checked}}" color="{{checked?color:''}}" disabled="{{disabled}}" class="i-radio-radio {{positionCls}}" />
      <view class="i-radio-title">{{label}}</view>
  </label>
</view>
